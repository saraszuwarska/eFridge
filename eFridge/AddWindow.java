package eFridge;

import java.awt.BorderLayout;
import java.awt.Color;
import java.awt.Dimension;
import java.awt.Font;
import java.awt.GridLayout;
import javax.swing.JButton;
import javax.swing.JComboBox;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JOptionPane;
import javax.swing.JPanel;
import javax.swing.JTextField;
import java.awt.FlowLayout;

import javax.print.attribute.standard.JobPrioritySupported;
import javax.swing.*;
import java.awt.event.*;
import java.util.concurrent.Flow;
/*
public class AddWindow extends JFrame implements ActionListener {

    JFrame frame = new JFrame();
    JPanel panel = new JPanel();

    JLabel name_label;
    JLabel name_label2;
    JLabel name_label3;
    JLabel name_label4;


    JComboBox categories = new JComboBox(categories_names);
    JTextField name_field = new JTextField();
    JTextField quantity_field = new JTextField();
    JTextField alertq_field = new JTextField();
    JButton submit_button = new JButton("DODAJ");

    AddWindow() {

        this.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        this.setSize(500, 300);
        this.setResizable(false);
        this.setLayout(new FlowLayout());
        this.getContentPane().setBackground(new Color(0xDCE6DD));

  

        name_label=new JLabel(name);
        name_label2=new JLabel(category);
        name_label3=new JLabel(quantity);
        name_label4=new JLabel(alertQuantity);

        this.add(name_label);
        this.add(name_label2);
        this.add(name_label3);
        this.add(name_label4);


        this.setVisible(true);


        /*
         * panel.setSize(new Dimension(500, 500));
         * this.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE); this.setSize(500,
         * 300); this.setResizable(false); this.setLayout(new FlowLayout());
         * this.getContentPane().setBackground(new Color(0xDCE6DD));
         * 
         * panel.setLayout(new GridLayout(0, 2));
         * 
         * name_field.setPreferredSize(new Dimension(250, 40));
         * name_field.setText("NAZWA"); name_field.addActionListener(this);
         * 
         * categories.setPreferredSize(new Dimension(250, 40));
         * categories.addActionListener(this);
         * 
         * quantity_field.setPreferredSize(new Dimension(250, 40));
         * quantity_field.setText("PODAJ ILOSC");
         * quantity_field.addActionListener(this);
         * 
         * alertq_field.setPreferredSize(new Dimension(250, 40));
         * alertq_field.setText("ALERT GDY"); alertq_field.addActionListener(this);
         * 
         * submit_button.addActionListener(this); submit_button.setFocusable(false);
         * 
         * panel.add(name_label); panel.add(name_field);
         * 
         * panel.add(name_label2); panel.add(quantity_field);
         * 
         * panel.add(name_label3); panel.add(alertq_field);
         * 
         * panel.add(name_label4); panel.add(categories);
         * 
         * panel.add(submit_button);
         * 
         * this.add(panel, BorderLayout.CENTER); this.setVisible(true);
         */
    }

   @Override
    public void actionPerformed(ActionEvent e) {
        // if (e.getSource() == submit_button) {
        // Product newItem = new Product();
        // newItem.name = name_field.getText();
        // newItem.category = categories.getSelectedIndex();
        // newItem.alertQuantity = 0;
        // newItem.quantity = 10;

        // }
    }

} 
*/
