package eFridge;

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        /*
         * fridgelist nowalista = new fridgelist(); Product item = new Product();
         * 
         * // dodawanie System.out.println("Podaj nazwe produktu: \n"); String name;
         * Scanner scanIn = new Scanner(System.in); name = scanIn.nextLine(); item.name
         * = name;
         * 
         * System.out.println("Podaj ilość produktu: \n"); String quantity; quantity =
         * scanIn.nextLine(); item.quantity = Integer.parseInt(quantity);
         * 
         * System.out.println("Podaj kiedy przypomnieć o zakupie: \n"); String
         * alertquantity; alertquantity = scanIn.nextLine(); item.alertQuantity =
         * Integer.parseInt(alertquantity); scanIn.close();
         * 
         * nowalista.products.add(item);
         * 
         * Frame ramka = new Frame(); ramka.adding(item); ramka.revalidate();
         */

        LaunchWindow newWindow = new LaunchWindow();

    }
}
